# Base URL of the API gateway. Defaults to http://localhost:8080 if unset
# (see src/api/httpClient.ts). Copy this file to .env.local to override for
# local development against a gateway running somewhere else.
VITE_API_BASE_URL=http://localhost:8080
