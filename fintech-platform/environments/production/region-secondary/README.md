# production / region-secondary

Multi-region is not implemented for this vertical slice -- one environment runs in one region. Placeholder for when `infrastructure/terraform/environments/disaster-recovery` and `docs/architecture/high-availability.md` are acted on.
