# production / security

Authentication and authorization are not implemented yet (see `security/authentication` and `security/authorization` -- still empty scaffolding, and "No auth" in `docs/architecture/vertical-slice.md`). This folder is a placeholder for `production`-specific identity provider config once that's built.
