# uat / messaging

Not part of the implemented vertical slice -- every inter-service call today is synchronous HTTP (see "Simplifications made on purpose" in `docs/architecture/vertical-slice.md`). Still empty scaffolding for when `messaging/kafka` is wired up; at that point this folder would hold the `uat` topic/consumer-group config.
