# uat / configuration

Cross-cutting configuration for `uat` lives in `config/application/uat.yaml` (platform defaults) and `config/feature-flags/uat.yaml` (what's turned on). Compliance rule overrides for this environment are in `config/compliance/*.yaml` under each file's `environments.uat` key.
