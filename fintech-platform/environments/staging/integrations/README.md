# staging / integrations

No third-party integrations are wired up in the implemented vertical slice yet (see `integrations/` at the repo root -- still empty scaffolding). This folder is a placeholder for `staging`-specific endpoints/credentials for those integrations once one is built.
