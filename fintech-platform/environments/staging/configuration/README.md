# staging / configuration

Cross-cutting configuration for `staging` lives in `config/application/staging.yaml` (platform defaults) and `config/feature-flags/staging.yaml` (what's turned on). Compliance rule overrides for this environment are in `config/compliance/*.yaml` under each file's `environments.staging` key.
