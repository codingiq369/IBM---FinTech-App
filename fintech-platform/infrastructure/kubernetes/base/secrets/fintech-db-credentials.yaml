# Managed by Kustomize. Base is environment-agnostic; overlays/<env>/patches/ supplies replicas, resources, image tags, and env-specific config.
# Placeholder only. Never commit a real password here.
# In every real environment this Secret's data is populated by an external
# secrets sync (see docs/architecture/architecture-decisions/ADR-0006-secrets-management.md
# and the `secrets` Terraform module in infrastructure/terraform/modules/secrets),
# not authored by hand or by kustomize.
apiVersion: v1
kind: Secret
metadata:
  name: fintech-db-credentials
type: Opaque
stringData:
  DB_USERNAME: CHANGEME
  DB_PASSWORD: CHANGEME
