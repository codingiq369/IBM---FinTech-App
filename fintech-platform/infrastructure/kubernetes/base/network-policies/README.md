# base/network-policies

Default-deny plus explicit allow rules for the ingress controller and inter-service calls (gateway -> the four backend services, accounts/transfers -> their downstream services).
