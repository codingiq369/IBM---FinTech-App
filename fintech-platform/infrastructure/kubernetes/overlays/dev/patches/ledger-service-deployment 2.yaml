# Managed by Kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-service
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: ledger-service
          image: ghcr.io/fintech-platform/ledger-service:dev-latest
          resources:
            requests:
              cpu: 50m
              memory: 192Mi
            limits:
              cpu: 250m
              memory: 384Mi
