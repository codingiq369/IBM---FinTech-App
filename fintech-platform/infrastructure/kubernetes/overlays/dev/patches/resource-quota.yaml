# Managed by Kustomize.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: fintech-quota
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "2Gi"
    limits.cpu: "2"
    limits.memory: "4Gi"
    pods: "10"
