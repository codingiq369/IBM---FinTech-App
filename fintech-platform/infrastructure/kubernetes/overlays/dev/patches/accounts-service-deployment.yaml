# Managed by Kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-service
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: accounts-service
          image: ghcr.io/fintech-platform/accounts-service:dev-latest
          resources:
            requests:
              cpu: 50m
              memory: 192Mi
            limits:
              cpu: 250m
              memory: 384Mi
