# Managed by Kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-service
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: accounts-service
          image: ghcr.io/fintech-platform/accounts-service:REPLACE_AT_DEPLOY
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1024Mi
