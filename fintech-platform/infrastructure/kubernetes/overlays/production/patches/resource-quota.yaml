# Managed by Kustomize.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: fintech-quota
spec:
  hard:
    requests.cpu: "6"
    requests.memory: "12Gi"
    limits.cpu: "12"
    limits.memory: "24Gi"
    pods: "40"
